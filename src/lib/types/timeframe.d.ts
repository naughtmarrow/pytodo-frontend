// this type is used only in the frontend to categorize todo objects based on due date for the sidebar separations
// or maybe not im not sure
export type TimeFrame = "TODAY" | "TOMORROW" | "WEEK" | "MONTH" | "LATER" | "NODATE"
